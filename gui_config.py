#
# This file contains configuration items used in other parts of the simulation
#
# Executable configuration files are frowned upon for most production code (due to
# the trouble malicious users might cause when changing settings). We are accepting
# that risk because it is so much quicker and easier than implementing a configuration
# file format and reader.
#


# GUI window width
WIDTH = 640

# GUI window height
HEIGHT = 480

# 1 unit of game space = SCALE units of GUI space
SCALE = 10.0

NPC_image = 'npc.gif'